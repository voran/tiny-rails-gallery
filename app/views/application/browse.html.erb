<div class="hero-unit">           
   <a href="/browse.html/">root</a>
   <% @entry.split('/').each do | path_chunk | %>
     <% 
       updir_urlencoded = ""
       updir = @entry.split(path_chunk)[0]
       if !updir.nil?
         updir.split('/').each do | updir_chunk |
           updir_urlencoded  =  updir_urlencoded  + u(updir_chunk) + '/'
         end
       end
     %>
     / <a href="/browse.html/<%= updir_urlencoded + u(path_chunk) %>/"><%= path_chunk %></a>
   <% end %>
</div>


<% @output[:subdirs].each do |subdir| %>
  <div class="span4">
    <img src="http://cdn.stoychev.info/icons/index/dir.png" width="150" height="150" alt="folder icon"/>
    <a href="<%=u subdir %>/"><%= subdir %></a>
  </div>
<% end %>

<% @output[:files].each do |file| %>
  <div class="span4">
    <img src="http://cdn.stoychev.info/icons/index/<%= file.split('.')[-1].downcase %>.png" width="150" height="150" alt="<%= file.split('.')[-1].downcase %>" />
    <a href="<%=u file %>"><%= file %>
  </div>
<% end %>